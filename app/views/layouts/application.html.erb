<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Appshelf</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1.5.10/css/pico.min.css" integrity="sha256-AmkBgnWRWgRJIBCpCCmwuc/mbOWTWKepkFXimo1nQqk=" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="container">
      <nav>
        <ul>
          <li><%= link_to "Appshelf", "/" %></li>
        </ul>
        <ul>
          <% if user_signed_in? %>
            <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to sign out?' } %>
          <% end %>
        </ul>
      </nav>
    </header>
    <main class="container">
      <ins><%= notice %></ins>
      <p class="danger"><%= alert %></p>
      <%= yield %>
    </main>
    <footer class="container">
      <small
        >Built with 💎 Ruby on Rails + <a href="https://picocss.com">Pico CSS</a>
        <br>
        by <a href="https://github.com/tomy0000000">Tomy Hsieh</a> at 🇹🇭 Bangkok, Thailand
        <br>
        <a href="https://github.com/tomy0000000/appshelf"
          >Source code</a
        ></small
      >
    </footer>
  </body>
</html>
